apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-api
  labels:
    app: auth-api
    component: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-api
  template:
    metadata:
      labels:
        app: auth-api
        component: backend
    spec:
      serviceAccountName: gke-secrets-sa
      containers:
      - name: auth-api
        image: asia-southeast1-docker.pkg.dev/trivpn-demo-prj/triztna/auth-api:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8081
          name: http
          protocol: TCP
        - containerPort: 9090
          name: metrics
          protocol: TCP
        env:
        - name: APP_ENV
          valueFrom:
            configMapKeyRef:
              name: ztna-config
              key: APP_ENV
        - name: MGMT_PORT
          valueFrom:
            configMapKeyRef:
              name: ztna-config
              key: MGMT_PORT
        - name: FREE_DOMAIN_SUFFIX
          valueFrom:
            configMapKeyRef:
              name: ztna-config
              key: FREE_DOMAIN_SUFFIX
        - name: DB_HOST
          valueFrom:
            secretKeyRef:
              name: database-secrets
              key: host
        - name: DB_PORT
          value: "5432"
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: database-secrets
              key: username
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: database-secrets
              key: password
        - name: DB_NAME
          valueFrom:
            secretKeyRef:
              name: database-secrets
              key: database
        - name: DB_SSL_MODE
          value: "require"
        - name: CACHE_HOST
          value: "valkey-service.tridorian-ztna.svc.cluster.local"
        - name: CACHE_PORT
          value: "6379"
        - name: CACHE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: cache-secrets
              key: password
        - name: ZTNA_PRIVATE_KEY
          valueFrom:
            secretKeyRef:
              name: ztna-secrets
              key: private-key
        - name: ZTNA_PUBLIC_KEY
          valueFrom:
            secretKeyRef:
              name: ztna-secrets
              key: public-key
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8081
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8081
          initialDelaySeconds: 10
          periodSeconds: 5
        securityContext:
          runAsNonRoot: true
          runAsUser: 1000
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
        volumeMounts:
        - name: ztna-secrets
          mountPath: "/mnt/secrets-store"
          readOnly: true
        - name: database-secrets
          mountPath: "/mnt/db-secrets"
          readOnly: true
        - name: cache-secrets
          mountPath: "/mnt/cache-secrets"
          readOnly: true
      volumes:
      - name: ztna-secrets
        csi:
          driver: secrets-store-gke.csi.k8s.io
          readOnly: true
          volumeAttributes:
            secretProviderClass: "ztna-secrets"
      - name: database-secrets
        csi:
          driver: secrets-store-gke.csi.k8s.io
          readOnly: true
          volumeAttributes:
            secretProviderClass: "database-secrets"
      - name: cache-secrets
        csi:
          driver: secrets-store-gke.csi.k8s.io
          readOnly: true
          volumeAttributes:
            secretProviderClass: "cache-secrets"
---
apiVersion: v1
kind: Service
metadata:
  name: auth-api
  labels:
    app: auth-api
spec:
  type: ClusterIP
  ports:
  - port: 8081
    targetPort: 8081
    protocol: TCP
    name: http
  - port: 9090
    targetPort: 9090
    protocol: TCP
    name: metrics
  selector:
    app: auth-api
